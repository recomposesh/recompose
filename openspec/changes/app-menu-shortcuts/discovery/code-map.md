# Code map

- `apps/desktop/src/main/menu/app-menu-template.ts` (electron main process (outside renderer FSD)) — symbols: buildAppMenuTemplate, AppMenuItem, AppMenuHandlers, AppMenuView, macApplicationMenu, macFileMenu, fileMenu, viewMenu, gatewayMenu, usageMenu, metricSubmenu, checklistToggleItem, showLogsItem, canvasCommandClick, usageCommandClick, leadingMenus, settingsItem, newGatewayItem. The whole menu bar shape lives here: every new item (Help menu, View navigation and surface toggles, Gateway lifecycle, split zoom group, Option-modified usage ranges) lands in this file, and AppMenuHandlers/AppMenuView grow the new seams and ticks.
- `apps/desktop/src/main/menu/app-menu-conductor.ts` (electron main process (outside renderer FSD)) — symbols: conductAppMenu, AppMenuConduct, repaint, reflectSettings, standOnUrl, reflectLogsDrawer, reflectUsageTable, storedChecklistChoice. Holds the mutable AppMenuView and reinstalls the menu on every change, so the new sidebar and inspector ticks need sibling reflect* methods here alongside reflectLogsDrawer and reflectUsageTable.
- `apps/desktop/src/main/menu/app-menu-boot.ts` (electron main process (outside renderer FSD)) — symbols: bootAppMenu. Binds the conductor's command seams to the window pushes (pushCanvasCommand, pushUsageCommand); new navigation, surface-toggle, and gateway-lifecycle commands get bound here.
- `apps/desktop/src/main/menu/app-menu.ts` (electron main process (outside renderer FSD)) — symbols: installAppMenu. The single Electron seam that turns the template into an installed Menu; the Dock menu needs a sibling installer beside it.
- `apps/desktop/src/main/index.ts` (electron main process (outside renderer FSD)) — symbols: gatewayLifecycle, trayMenuHandlers, appMenu, bootAppMenu, trayMenuWiring, createGatewayLifecycleRequests. Boot wiring: builds gatewayLifecycle (line 57), the tray handlers (line 63), and the app menu (line 83), and calls optimizer.watchWindowShortcuts(window, { zoom: true }) at line 254, which is the @electron-toolkit/utils guard that swallows the packaged reload keystroke the View menu advertises.
- `apps/desktop/src/main/engine-host/gateway-lifecycle-requests.ts` (electron main process (outside renderer FSD)) — symbols: createGatewayLifecycleRequests. Supplies start/stop/restart per slug already consumed by the tray; the Gateway menu's lifecycle items and the Dock menu reuse this same factory.
- `apps/desktop/src/main/tray/tray-menu-template.ts` (electron main process (outside renderer FSD)) — symbols: buildTrayMenuTemplate, TrayMenuHandlers, TrayMenuInput, TrayMenuItem, TrayGateway, TrayIconSource, TrayLifecycleIcons. The per-gateway start/stop/restart submenu shape the macOS Dock menu is meant to mirror; TrayGateway and TrayMenuItem are the reusable value types.
- `apps/desktop/src/main/tray/tray-wiring.ts` (electron main process (outside renderer FSD)) — symbols: trayMenuWiring. Maps lifecycle seams onto TrayMenuHandlers (onStartGateway, onStopGateway, onRestartGateway); the Dock menu wiring follows this exact shape.
- `apps/desktop/src/main/tray/menu-bar-tray.ts` (electron main process (outside renderer FSD)) — symbols: showMenuBarTray, refreshMenuBarTray, hideMenuBarTray, isMenuBarTrayVisible. Owns the tray's lifetime and its refresh on gateway/engine change; the Dock menu needs the same refresh trigger so its submenus stay current.
- `apps/desktop/src/main/windows/renderer-url.ts` (electron main process (outside renderer FSD)) — symbols: onGatewayDetailUrl, onUsageUrl, rendererUrlFor, rendererBaseFor, settingsShortcutRouteFor, newGatewayRouteFor, SETTINGS_SHORTCUT_ROUTE. Decides which surface the window stands on, which is how the Gateway and Usage menus come and go; View-menu navigation to gateways, providers, and usage needs route constants here.
- `apps/desktop/src/main/ipc/push-events.ts` (electron main process (outside renderer FSD)) — symbols: pushCanvasCommand, pushUsageCommand, pushSettingsChanged, pushDevtoolsToggle. The only path a menu command travels to the renderer; new navigation and surface-toggle commands need push functions here.
- `apps/desktop/src/main/ipc/system-ipc.ts` (electron main process (outside renderer FSD)) — symbols: createSystemIpcHandlers, SystemIpcContext, SystemIpcHandlers, openConfigFolder. Answers system:open-config-folder and system:logs-drawer; the Help menu's config-folder item reuses openConfigFolder, and new sidebar/inspector report channels are handled here.
- `apps/desktop/src/main/ipc/register-ipc.ts` (electron main process (outside renderer FSD)) — symbols: noteLogsDrawer. Line 115 hands appMenu.reflectLogsDrawer into the system handlers as noteLogsDrawer; new sidebar and inspector report channels join the same seam.
- `apps/desktop/src/main/ipc/dispatch.ts` (electron main process (outside renderer FSD)) — symbols: system:open-config-folder, system:logs-drawer, system:usage-table. The channel allowlist a request must appear in; any new report channel is dead until it is listed here.
- `packages/contracts/src/ipc.ts` (contracts package (outside renderer FSD)) — symbols: ipcEvents, ipcChannels, canvas:command, usage:command, system:logs-drawer, system:usage-table, IpcEventPayload, IpcEvent, IpcChannel, RecomposeIpcEvents, RecomposeIpc. The wire vocabulary: canvas:command (line 212) enumerates zoom-in/zoom-out/zoom-to-fit/tidy/toggle-logs with no reset-zoom member, and usage:command (lines 215-225) carries only range-24h/7d/30d, so both enums plus new push commands and report channels change here.
- `packages/contracts/src/ipc.test.ts` (contracts package (outside renderer FSD)) — symbols: none. Pins the channel table; new commands and report channels land red here first.
- `packages/contracts/src/ipc-events.test.ts` (contracts package (outside renderer FSD)) — symbols: none. Pins the event payload enums that the canvas and usage command changes widen.
- `packages/contracts/src/ipc.test-d.ts` (contracts package (outside renderer FSD)) — symbols: none. Type-level spec for the derived IpcRequest/IpcResponse/IpcEventPayload maps the new channels feed.
- `packages/contracts/src/index.ts` (contracts package (outside renderer FSD)) — symbols: none. Package barrel every main and renderer consumer imports the new command and channel types through.
- `apps/desktop/src/preload/index.ts` (preload bridge (outside renderer FSD)) — symbols: canvas:command, usage:command, system:logs-drawer, system:usage-table. Exposes the request and event channels onto window.recompose and window.recomposeEvents; every new command or report channel needs a bridge entry.
- `apps/desktop/src/preload/index.d.ts` (preload bridge (outside renderer FSD)) — symbols: none. Declares the bridge shape the renderer types against, so it tracks any channel addition.
- `apps/desktop/src/renderer/src/app/routes/__root.tsx` (app) — symbols: none. The shell that mounts the sidebar and the surface outlet, and the natural place a menu-driven navigation command is answered.
- `apps/desktop/src/renderer/src/app/routes/-app-sidebar.tsx` (app) — symbols: none. The sidebar the new View menu tick toggles and reports on.
- `apps/desktop/src/renderer/src/app/routes/-app-toolbar.tsx` (app) — symbols: AppToolbar. Hosts the sidebar and inspector toggles in the window band, so its state is what the menu tick must agree with.
- `apps/desktop/src/renderer/src/app/routes/-surface-request.ts` (app) — symbols: surfaceRequest, withSheet, withoutSheet, RootSearch. Reads and writes the root search a surface request travels in; menu navigation reuses it rather than inventing a second address shape.
- `apps/desktop/src/renderer/src/app/routes/gateways.$slug.tsx` (app) — symbols: none. The gateways surface the View menu's first number accelerator navigates to.
- `apps/desktop/src/renderer/src/app/routes/providers.tsx` (app) — symbols: none. The providers surface the View menu's second number accelerator navigates to.
- `apps/desktop/src/renderer/src/app/routes/usage.tsx` (app) — symbols: none. The usage surface the View menu's third number accelerator navigates to, and the route whose search the Usage menu's ranges rewrite.
- `apps/desktop/src/renderer/src/app/routes/index.tsx` (app) — symbols: none. The home surface, which decides where a navigation command lands when no gateway is chosen.
- `apps/desktop/src/renderer/src/shared/lib/visibility/sidebar-visibility.ts` (shared) — symbols: subscribeToSidebarVisibility, sidebarHidden, showSidebar, hideSidebar. The external store the sidebar tick reads and the toggle command drives; the report to main reads sidebarHidden.
- `apps/desktop/src/renderer/src/shared/lib/visibility/inspector-visibility.ts` (shared) — symbols: subscribeToInspectorVisibility, inspectorOpen, openInspector, closeInspector, toggleInspector. The matching inspector store, already carrying toggleInspector, which the View menu's inspector item calls and reports back from.
- `apps/desktop/src/renderer/src/shared/lib/index.ts` (shared) — symbols: none. Segment public API for shared/lib; the visibility helpers reach consumers only through this barrel.
- `apps/desktop/src/renderer/src/shared/ui/sidebar-toggle/sidebar-toggle.tsx` (shared) — symbols: SidebarToggle. The in-window sidebar control whose state the new menu tick must mirror, so both read one store.
- `apps/desktop/src/renderer/src/shared/ui/inspector-toggle/inspector-toggle.tsx` (shared) — symbols: InspectorToggle. The in-window inspector control the menu tick pairs with.
- `apps/desktop/src/renderer/src/shared/ui/index.ts` (shared) — symbols: none. Segment public API for shared/ui, the import path for both toggles.
- `apps/desktop/src/renderer/src/shared/ui/copy-button/copy-button.tsx` (shared) — symbols: none. The existing clipboard control; the Gateway menu's copy-base-URL item should reuse its copy path rather than a second clipboard call.
- `apps/desktop/src/renderer/src/shared/testing/fake-bridge.ts` (shared) — symbols: none. The fake window.recompose bridge every browser test and story runs against, so it grows a stub for each new channel.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/lib/use-canvas-commands.ts` (pages) — symbols: CanvasCommands. Subscribes the canvas to canvas:command and performs zoom and tidy; the split reset-to-100% command is answered here.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-canvas-page/canvas-page-hooks.ts` (pages) — symbols: useLogsCommand, useMenuReadsTheDrawer, useInspectorPutAwayOnLeave, useSelectionPutAwayWithInspector. useMenuReadsTheDrawer is the existing pattern for reporting renderer state back to the menu, which the sidebar and inspector reports copy.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-canvas-page/removal-flow.ts` (pages) — symbols: removalAsked, forgottenEverywhere, RemovalAsked. The existing delete confirmation the Gateway menu's delete item must route through rather than deleting outright.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/endpoint-box/endpoint-box.tsx` (pages) — symbols: none. Where the gateway base URL is presented today, so the menu's copy item takes its URL from the same derivation.
- `apps/desktop/src/renderer/src/pages/usage/ui/usage-page/usage-page.tsx` (pages) — symbols: usage:command, system:usage-table. Subscribes to usage:command at line 178 and reports the table twin at line 220; the widened range set is answered here.
- `apps/desktop/src/renderer/src/pages/usage/ui/usage-page/usage-page-moves.ts` (pages) — symbols: movedSearch. Translates one usage command into the next search value; every new range command extends this single mapping.
- `apps/desktop/src/renderer/src/pages/usage/lib/usage-search.ts` (pages) — symbols: USAGE_SEARCH_RANGES, UsageSearchRange, PresetRange, withRange, usageSearchFrom, spendSnappedRange. USAGE_SEARCH_RANGES holds 1h, 24h, 7d, 30d, this-week, this-month, custom, which is the full ledger range list the Usage menu is meant to grow to; only 24h, 7d, and 30d have menu items today.
- `apps/desktop/src/main/menu/app-menu-template.test.ts` (electron main process (outside renderer FSD)) — symbols: none. The behavior spec for the menu bar's shape per platform, where the Help menu and View items land red first.
- `apps/desktop/src/main/menu/app-menu-gateway.test.ts` (electron main process (outside renderer FSD)) — symbols: none. Covers the Gateway menu, so the lifecycle items and the split zoom group are specified here.
- `apps/desktop/src/main/menu/app-menu-usage.test.ts` (electron main process (outside renderer FSD)) — symbols: none. Covers the Usage menu's items and accelerators, which the Option-modified ranges rewrite.
- `apps/desktop/src/main/menu/app-menu-conductor.test.ts` (electron main process (outside renderer FSD)) — symbols: none. Covers the view state and repaint discipline the new sidebar and inspector ticks join.
- `apps/desktop/src/main/menu/app-menu-conductor-usage.test.ts` (electron main process (outside renderer FSD)) — symbols: none. Covers the usage-surface half of the conductor's view state.
- `apps/desktop/src/main/menu/app-menu-template.testkit.ts` (electron main process (outside renderer FSD)) — symbols: none. Shared builders the menu specs read templates through; new menus need matching finders here.
- `apps/desktop/src/main/tray/tray-menu-template.test.ts` (electron main process (outside renderer FSD)) — symbols: none. The spec the Dock menu's mirrored submenu shape is measured against.
