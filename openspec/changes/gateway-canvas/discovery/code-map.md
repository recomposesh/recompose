# Code map

- `apps/desktop/src/renderer/src/pages/gateway-canvas/index.ts` (pages (slice: gateway-canvas, public API)) — symbols: GatewayCanvasPage. The slice public API the route imports; a canvas surface must still leave this the single export point.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-canvas-page/gateway-canvas-page.tsx` (pages (slice: gateway-canvas, segment: ui)) — symbols: GatewayCanvasPage. Composition root that resolves the slug to a gateway and splits stage from inspector; the canvas replaces what it hands GatewayStage.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-stage/gateway-stage.tsx` (pages (slice: gateway-canvas, segment: ui)) — symbols: GatewayStage. Today a single selectable gateway card; this is the surface that becomes the node canvas with cables and the plus affordance.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-drawer/gateway-drawer.tsx` (pages (slice: gateway-canvas, segment: ui)) — symbols: GatewayDrawer. Inspector that currently owns the serves list and endpoint box; cable gestures must stay consistent with the drawer's view of the same gateway.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/add-model-flow/add-model-flow.tsx` (pages (slice: gateway-canvas, segment: ui)) — symbols: AddModelFlow, AddModelFlowProps. Form path for defining a virtual model and its target; the plus affordance and cable drop reuse or replace this entry point.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/model-fields/model-fields.tsx` (pages (slice: gateway-canvas, segment: ui)) — symbols: ModelFields, ModelFieldsProps. Name, alias, target and provider-model fields; a node-side editor draws on the same field set.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/option-list/option-list.tsx` (pages (slice: gateway-canvas, segment: ui)) — symbols: OptionList, OptionRow, OptionGroup. Grouped picker for targets and provider models; a cable drop that ends on empty space needs the same option vocabulary.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/served-model-row/served-model-row.tsx` (pages (slice: gateway-canvas, segment: ui)) — symbols: ServedModelRow. Row rendering one virtual model and its target standing; the node representation of a virtual model mirrors its states.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/model/served-models.ts` (pages (slice: gateway-canvas, segment: model)) — symbols: servedModels, servesTally, ServedModel, ServedTarget. Joins gateway virtual models to stored accounts and marks removed targets; this is the read model the canvas turns into nodes and cables.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/lib/target-groups.ts` (pages (slice: gateway-canvas, segment: lib)) — symbols: targetGroups. Groups accounts into pickable target options, so target nodes on the canvas can be grouped the same way.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/lib/model-draft.ts` (pages (slice: gateway-canvas, segment: lib)) — symbols: nameRefusal, idFollowingName, idRefusal, discoveryHint, SettledDefinition, emptyDefinition, draftKept, gatewayDefining, DraftBinding, servesPreview, ModelListReading, modelListReading, refusalFromMain. Pure draft rules for a virtual-model definition; a cable-drawn connection must land through the same validation rather than a parallel path.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/lib/use-held-draft.ts` (pages (slice: gateway-canvas, segment: lib)) — symbols: useHeldDraft, HeldDraft. Holds an in-flight definition above the drawer so closing the inspector does not discard it; an in-flight cable drag needs the same survival rule.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/lib/use-inspector-reveal.ts` (pages (slice: gateway-canvas, segment: lib)) — symbols: useInspectorReveal. Mount and leave timing for the inspector; canvas transitions coordinate with this rather than adding a second animation clock.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/lib/use-press-away.ts` (pages (slice: gateway-canvas, segment: lib)) — symbols: usePressAway. Closes the inspector on a press outside the surface; canvas pointer gestures must not read as press-away and dismiss the inspector mid-drag.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/lib/inspector-width.ts` (pages (slice: gateway-canvas, segment: lib)) — symbols: inspectorWidth. Reads the stored inspector width; the canvas viewport width is what remains after it.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/testing/gateway-canvas.testkit.ts` (pages (slice: gateway-canvas, segment: testing)) — symbols: storedAccounts, accountsWithout, servingGateway, freshGateway, runningGateway, listedModels. Seeds every browser test in the slice; canvas specs extend these fixtures instead of inventing new gateway shapes.
- `apps/desktop/src/renderer/src/app/routes/gateways.$slug.tsx` (app (segment: routes)) — symbols: Route. File route that mounts the page for a slug; canvas viewport or selection state persisted in search params is registered here.
- `apps/desktop/src/renderer/src/app/routes/__root.tsx` (app (segment: routes)) — symbols: Route, RouterAppContext. Root route and router context carrying the query client; a canvas that warms data through a loader reads this context.
- `apps/desktop/src/renderer/src/app/styles/theme.css` (app (segment: styles)) — symbols: none. Semantic theme tokens for both schemes; cable, port and node colors land here rather than as literal class values.
- `apps/desktop/src/renderer/src/app/styles/primitives.css` (app (segment: styles)) — symbols: none. Primitive token scale the theme layer draws from; a new canvas value is added here rather than snapped to a near neighbour.
- `apps/desktop/src/renderer/src/shared/api/gateways.ts` (shared (segment: api)) — symbols: gatewaysQueryOptions, fetchOfferedPort, useSaveGateway, useDefineVirtualModel, useMoveGatewayPort. The read model and the mutations the canvas gestures invoke; useDefineVirtualModel is what a completed cable drop calls.
- `apps/desktop/src/renderer/src/shared/api/accounts.ts` (shared (segment: api)) — symbols: accountsQueryOptions, bindAccountChangesToCache, runtimeStandingQueryOptions, useRemoveAccount. Stored accounts feeding target nodes, plus the cache binding that keeps those nodes live when accounts change.
- `apps/desktop/src/renderer/src/shared/api/provider-models.ts` (shared (segment: api)) — symbols: providerModelsQueryOptions, ProviderModelList. Per-account model listing behind target selection; a cable landing on a target still needs a chosen provider model.
- `apps/desktop/src/renderer/src/shared/api/index.ts` (shared (segment: api, public API)) — symbols: none. Segment barrel that re-exports with `export *` only and declares no named symbol of its own, so cite the concrete module for any symbol.
- `apps/desktop/src/renderer/src/shared/lib/inspector-visibility.ts` (shared (segment: lib)) — symbols: subscribeToInspectorVisibility, inspectorOpen, toggleInspector. External store for inspector open state; selecting a node on the canvas drives the same store rather than local React state.
- `apps/desktop/src/renderer/src/shared/lib/panel-width.ts` (shared (segment: lib)) — symbols: subscribeToPanelWidths, panelWidth, setPanelWidth, keepPanelWidth. Persisted panel widths; the canvas re-measures its viewport when the inspector resizes through these.
- `apps/desktop/src/renderer/src/shared/lib/panel-resize.ts` (shared (segment: lib)) — symbols: PanelBounds, panelBounds, PanelName, PanelStanding, draggedPanel, restoredPanel, steppedPanel. Resize arithmetic and bounds for the inspector split; canvas layout must respect the same collapsed and sized standings.
- `apps/desktop/src/renderer/src/shared/ui/index.ts` (shared (segment: ui, public API)) — symbols: Badge, Chip, Icon, IconName, PanelSeparator, StatusChip, StatusIndicator, stateMark, stateWord, placeFocus. Design-system barrel the canvas draws node chrome from; a new canvas primitive is exported here with its stories sibling.
- `apps/desktop/src/renderer/src/shared/ui/icon/icon.tsx` (shared (segment: ui)) — symbols: Icon, IconName. Glyph set behind node and port affordances; the plus affordance and any port marker add names to IconName here.
- `apps/desktop/src/renderer/src/widgets/gateway/toolbar/index.ts` (widgets (slice group: gateway, slice: toolbar)) — symbols: GatewayToolbar. Toolbar above the gateway surface; canvas-level controls such as zoom or fit belong beside it, not inside the page slice.
- `apps/desktop/src/renderer/src/widgets/gateway/sidebar/index.ts` (widgets (slice group: gateway, slice: sidebar)) — symbols: GatewaySidebar. Gateway list that selects which composition the canvas draws; selection continuity across gateways runs through it.
- `apps/desktop/src/renderer/src/entities/account/index.ts` (entities (slice: account, public API)) — symbols: accountMark, accountName. Display mark and name for an account; target nodes label themselves through these instead of re-deriving provider naming.
- `apps/desktop/src/renderer/src/shared/testing/fake-gateways.ts` (shared (segment: testing)) — symbols: GatewayHandlers, GatewaySeed, gatewaySeed, forgetEngineStateListeners, listenForEngineStates, emitEngineStates, gatewayHandlers. Fake bridge handlers and gateway seeds; canvas browser tests and stories drive engine state through these rather than real IPC.
- `apps/desktop/src/renderer/src/shared/testing/index.ts` (shared (segment: testing, public API)) — symbols: noAccounts, installFakeBridge, BridgeParameters, emitEngineStates, gatewaySeed, paintedBox, paintedStyle, pressedByKeyboard. Testing barrel including paintedBox and paintedStyle, the measuring helpers a cable-geometry assertion needs from the page.
- `packages/contracts/src/gateway-config.ts` (packages/contracts (shared contract package, outside the renderer FSD tree)) — symbols: GATEWAY_CONFIG_VERSION, gatewayConfigSchema, GatewayConfig, virtualModelSchema, VirtualModel, targetSchema, Target, modelAliasSchema, modelAliasFromName, slugFromName, loadGatewayConfig. The persisted topology the canvas draws; node and cable identity derive from VirtualModel and Target, and no new persisted shape is needed for a read-only canvas.
- `packages/contracts/src/accounts.ts` (packages/contracts (shared contract package, outside the renderer FSD tree)) — symbols: accountSchema, Account, AccountsDocument, AccountKind, accountKindSchema, loadAccountsDocument. Account shapes behind every target node, including the kinds that group targets on the canvas.
- `apps/desktop/package.json` (apps/desktop (package manifest, outside the FSD tree)) — symbols: none. Gap: no graph or canvas dependency is declared today (no reactflow, xyflow, d3 or konva match), so a chosen library lands here and needs an ADR first.
- `openspec/specs/virtual-models/spec.md` (openspec (living spec, outside the code tree)) — symbols: none. Behaviour already frozen for defining a virtual model and its target; canvas gestures must not restate or contradict these requirements.
- `openspec/specs/gateways/spec.md` (openspec (living spec, outside the code tree)) — symbols: none. Gateway detail and selection behaviour the canvas screen inherits.
- `openspec/changes/archive/2026-08-08-gateway-virtual-models/proposal.md` (openspec (archived change, outside the code tree)) — symbols: none. Gap: the gateway-canvas change folder (proposal.md, and no specs/ delta exists yet) sits only on the feature worktree and resolves to nothing in the validated checkout, so this prior slice is the citable record of the composition work the canvas builds on; the proposal's design reference `templates/gateway/index.html` also resolves to nothing in this repository and lives in the Claude Design project.
