# Code map

- `openspec/changes/onboarding-wizard/specs/onboarding/spec.md` (spec artifact (outside FSD)) — symbols: none. The two ADDED requirements this map serves: first-session-only gating with no stored step, and a served request as the finish signal.
- `openspec/changes/onboarding-wizard/manifest.md` (spec artifact (outside FSD)) — symbols: none. Declares tier full, isUI true, and the affected subsystem list that drove this search.
- `packages/contracts/src/settings.ts` (contracts package (outside FSD)) — symbols: SETTINGS_VERSION, settingsSchema, Settings, settingsPatchSchema, SettingsPatch, withSettingsPatch, loadSettings, defaultSettings, firstRequestServed, showOnboardingChecklist. Holds the only two onboarding fields on disk today, so a wizard standing that survives relaunch needs a new field here plus a version 7 migration.
- `packages/contracts/src/engine-traffic.ts` (contracts package (outside FSD)) — symbols: requestOutcomeSchema. Defines the live, served and failed outcomes; the spec requires the wizard to finish on served and stay put on a refusal.
- `packages/contracts/src/ipc.ts` (contracts package (outside FSD)) — symbols: settings:get, settings:save, settings:changed. The three channels the renderer wizard rides to read the standing, write it, and hear it change from the menu or another window.
- `apps/desktop/src/main/storage/settings-amend.ts` (main process (outside FSD)) — symbols: amendStoredSettings, recordFirstRequestServed, firstRequestReporter. Writes the once-only first-request record and pushes the amended document back to the renderer, so a wizard standing writes through the same amend path.
- `apps/desktop/src/main/engine-host/first-request.ts` (main process (outside FSD)) — symbols: noticingTheFirstGrant. Gap the feature must close: the latch fires when a spend grant resolves, not when the gateway records the outcome as served, which the spec makes the finish signal.
- `apps/desktop/src/main/boot/stored-boot.ts` (main process (outside FSD)) — symbols: StoredBoot, storedSpendGrants. Wires noticingTheFirstGrant to firstRequestReporter and to deps.reflectSettings, so moving the signal from grant to outcome lands in this wiring.
- `apps/desktop/src/main/storage/settings-store.ts` (main process (outside FSD)) — symbols: loadSettingsFile, saveSettingsFile. The file read and write behind every amend; cited from its import in settings-amend.ts rather than opened, so treat its full export list as unverified.
- `apps/desktop/src/main/menu/view-menu.ts` (main process (outside FSD)) — symbols: viewMenu, checklistToggleItem. Carries the Show Onboarding Checklist checkbox today, the nearest existing home for the menu way back into setup the spec demands.
- `apps/desktop/src/main/menu/app-menu-item.ts` (main process (outside FSD)) — symbols: AppMenuItem, AppMenuHandlers, AppMenuView, onToggleChecklist, checklistShown. The handler and view contract a reopen-the-wizard menu command extends, since every menu row reads its checked state off AppMenuView.
- `apps/desktop/src/main/menu/app-menu-conductor.ts` (main process (outside FSD)) — symbols: AppMenuConduct, conductAppMenu. Reflects showOnboardingChecklist into view.checklistShown and saves the toggle back, so a reopen command joins the same conduct.
- `apps/desktop/src/main/menu/app-menu-template.ts` (main process (outside FSD)) — symbols: buildAppMenuTemplate, AppMenuHandlers, AppMenuItem, AppMenuView. Assembles the whole menu from the per-menu builders, so a new setup row shows up in its template test.
- `apps/desktop/src/main/menu/app-menu-boot.ts` (main process (outside FSD)) — symbols: none. Boots the menu conduct; listed by path only because the read budget ran out before I opened it, so its exports are unverified.
- `apps/desktop/src/renderer/src/app/routes/__root.tsx` (app) — symbols: Route, RootLayout, RouterAppContext, usePushedCaches, surfaceMain, useWindowBand. The shell a full-window wizard must sit over; its loader already ensures the settings query, so the opening-step decision can read the profile before first paint.
- `apps/desktop/src/renderer/src/app/routes/index.tsx` (app) — symbols: Route, HomeRoute. The canvas entry that redirects to the remembered gateway, so a wizard gate has to decide before this beforeLoad redirect runs.
- `apps/desktop/src/renderer/src/app/routes/-app-sidebar.tsx` (app) — symbols: AppSidebar, GetStartedPanel. The single mount point of the existing checklist panel, which the wizard either supersedes or has to stay consistent with.
- `apps/desktop/src/renderer/src/app/router.tsx` (app) — symbols: none. Router construction and context; cited by path only, not opened within the read budget.
- `apps/desktop/src/renderer/src/app/routeTree.gen.ts` (app) — symbols: none. Generated route tree that regenerates if the wizard takes a route of its own rather than an overlay.
- `apps/desktop/src/renderer/src/shared/api/settings.ts` (shared) — symbols: settingsQueryOptions, bindSettingsToCache, useSettingsWriter. Verified at lines 10, 21 and 32: the read, the push binding and the optimistic writer, which is how the wizard reads and records its standing; the SettingsWriter shape stays local and unexported.
- `apps/desktop/src/renderer/src/shared/api/index.ts` (shared) — symbols: none. Corrected after rejection: this segment public API is ten wildcard export-star lines and declares no symbol of its own, so the settings names resolve through its './settings' re-export.
- `apps/desktop/src/renderer/src/shared/ui/index.ts` (shared) — symbols: Sheet, SheetActionSlot, Button, ConsequenceDialog, Icon, BrandMark, StatusIndicator, PageError. The kit a wizard composes from; note there is no full-window overlay primitive here, so the wizard container is new work.
- `apps/desktop/src/renderer/src/widgets/get-started/lib/get-started-steps.ts` (widgets) — symbols: getStartedSteps, GetStartedStep, GetStartedProgress. Already derives the current step from stored documents rather than remembered state, which is exactly the opening-step rule the spec restates for the wizard.
- `apps/desktop/src/renderer/src/widgets/get-started/ui/get-started-panel/get-started-panel.tsx` (widgets) — symbols: GetStartedPanel, sessionSteps, usePanelReveal. Reads settings.firstRequestServed and clears showOnboardingChecklist on dismissal, the dismissal path the wizard's own standing must not contradict.
- `apps/desktop/src/renderer/src/widgets/get-started/index.ts` (widgets) — symbols: GetStartedPanel. Slice public API, currently a single export, which grows or retires depending on whether the wizard absorbs the checklist.
- `apps/desktop/src/renderer/src/widgets/get-started/lib/get-started-celebration.ts` (widgets) — symbols: none. Completion animation helper for the checklist; path cited from a directory listing, contents not opened, so its exports are unverified.
- `apps/desktop/src/renderer/src/widgets/get-started/lib/get-started-collapse.ts` (widgets) — symbols: none. Collapse behavior for the checklist panel; path cited from a directory listing, contents not opened, so its exports are unverified.
- `apps/desktop/src/renderer/src/widgets/gateway/create/index.ts` (widgets) — symbols: CreateGatewaySheet. The create-a-gateway sheet the root already mounts, the reusable act behind the wizard's first step.
- `apps/desktop/src/renderer/src/widgets/gateway/sidebar/index.ts` (widgets) — symbols: none. Gateway sidebar slice inside the gateway slice group; path cited from a barrel listing, exports unverified.
- `apps/desktop/src/renderer/src/widgets/gateway/toolbar/index.ts` (widgets) — symbols: none. Gateway toolbar slice inside the gateway slice group; path cited from a barrel listing, exports unverified.
- `apps/desktop/src/renderer/src/widgets/provider/sidebar/index.ts` (widgets) — symbols: none. Provider sidebar slice; path cited from a barrel listing, exports unverified.
- `apps/desktop/src/renderer/src/pages/providers/index.ts` (pages) — symbols: AddProviderAct, ProvidersPage. Where the connect-a-provider step lands; AddProviderAct is already lifted into the window strip by the root route, so the wizard can reuse it.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/index.ts` (pages) — symbols: GatewayCanvasPage. The canvas the wizard resolves into, and the surface that carries virtual models, routers and targets for the compose step.
- `apps/desktop/src/renderer/src/pages/home/index.ts` (pages) — symbols: HomePage. The empty state a profile with no gateway sees today, the screen the wizard displaces on a first session.
- `apps/desktop/src/renderer/src/pages/settings/index.ts` (pages) — symbols: SettingsPage. Settings screen; if the wizard standing becomes visible or resettable outside the menu, it lands in one of this slice's sections.
- `apps/desktop/src/renderer/src/pages/usage/ui/usage-page/use-usage-view.ts` (pages) — symbols: firstRequestServed. The one other renderer reader of firstRequestServed, so any change to that field's meaning has to keep this view honest.
- `apps/desktop/src/renderer/src/entities/account/index.ts` (entities) — symbols: AccountKind, accountKinds, accountKindName, accountKindTitle, accountsOfKind, accountsStandingAsTarget, offeredAccountKind. The account vocabulary the connect-a-provider step needs to offer api keys, subscriptions, aggregators and local runtimes as one list.
- `apps/desktop/src/renderer/src/entities/request-log/index.ts` (entities) — symbols: requestFailed, requestInFlight, trafficAggregates, TrafficAggregates. Reads request standing in the renderer, the layer that tells a waiting wizard step whether a request served or was refused.
- `apps/desktop/src/preload/index.ts` (preload (outside FSD)) — symbols: none. Exposes window.recompose and window.recomposeEvents; path cited only, not opened, so no symbol is claimed.
- `apps/desktop/src/preload/index.d.ts` (preload (outside FSD)) — symbols: none. Types the bridge the wizard calls through; path cited only, not opened.
- `apps/desktop/src/renderer/src/app/testing/render-app.tsx` (app) — symbols: none. The harness every browser test mounts the shell through, so a wizard gate over the shell changes what this renders by default.
- `designs/recompose.pen` (design artifact (outside FSD)) — symbols: none. Carries the wizard frames and their dark twins; encrypted, so read it through the pencil MCP tools and never with Read or Grep.
- `apps/desktop/src/renderer/src/pages` (pages) — symbols: none. Gap: no onboarding slice exists under pages or widgets today, so the wizard slice is entirely new and its layer choice is an open decision for the design phase.
- `openspec/specs` (spec artifact (outside FSD)) — symbols: none. Gap: the seventeen capability directories here include no onboarding, so this change opens a new capability rather than amending one.
- `apps/desktop/e2e/features` (e2e (outside FSD)) — symbols: none. Gap: the fifteen feature directories here include no onboarding, so the wizard scenarios need a new directory and step definitions.
