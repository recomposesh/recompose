# Tasks

## 1. The field that separates a first session

- [x] 1.1 Specify `setupWizardSettled` on the settings schema, and widen the type spec in the same
      commit
- [x] 1.2 Specify the migration from 6 writing `true`, so an upgraded profile never meets the
      wizard
- [x] 1.3 Specify `setupOpensOn`: a settled profile opens on nothing, and an unsettled one opens on
      the step its own gateways justify
- [x] 1.4 Name schema 7 and the field in the published settings reference

## 2. The surface that holds the window

- [x] 2.1 Specify Escape and an outside press both leaving the surface standing
- [x] 2.2 Specify the surface claiming the whole window and painting its own drag band, so the
      window still moves
- [x] 2.3 Specify the surface naming the step it stands on for a screen reader
- [x] 2.4 Report the surface's standing into the visibility store the View menu already reads
- [x] 2.5 Specify the View menu item that opens setup again, on every platform
- [x] 2.6 Specify a step taller than the window scrolling its content and never its acts

## 3. The first served request

- [x] 3.1 Specify `noticingTheFirstServed` latching on a `served` outcome rather than on a spend
      grant
- [x] 3.2 Specify the traffic desk handing each parsed report to an observer
- [x] 3.3 Specify the latch firing once over a run of served requests
- [x] 3.4 Wire the observer through stored boot, replacing the grant-shaped latch

## 4. The rank that picks a first model

- [x] 4.1 Specify the rank promoting a large model and demoting a small one
- [x] 4.2 Specify version digits breaking a tie, with any date stamp stripped first
- [x] 4.3 Specify a listing of only small models still yielding a model, because a rank never
      refuses
- [x] 4.4 Specify the model id following the harnesses picked: `claude-my-model`, else `my-model`
- [x] 4.5 Probe the rank against every listing this repository carries

## 5. The graph and the run

- [x] 5.1 Specify the graph standing a round-robin router at the entry even over one target
- [x] 5.2 Specify seats named by place, so a rebuild writes the same graph rather than a second one
- [x] 5.3 Specify `standingOf` reading each job as finished, running, refused, or waiting
- [x] 5.4 Specify a refused job stopping the run where it stood, with the finished jobs kept
- [x] 5.5 Specify the gateway and its virtual model reaching disk in one write
- [x] 5.6 Specify the run reporting the refusal the main process wrote, rather than one line for
      every reason
- [x] 5.7 Specify the name counting past every gateway name the directory holds
- [x] 5.8 Specify the run reading the directory again after a refused name, then writing under the
      next free one
- [x] 5.9 Specify one write per attempt however many times the effect fires
- [x] 5.10 Specify a source the recording never produced staying out of the targets

## 6. The steps

- [x] 6.1 The welcome step, which asks nothing and so offers no skip
- [x] 6.2 The harness step, with the catalog in its groups and the count it reports
- [x] 6.3 The sources step, over what the machine already holds and what a person connects
- [x] 6.4 Specify a source arriving marked once its account lands, and one already stored waiting
      on the person
- [x] 6.5 The compose step, drawing the graph before it exists
- [x] 6.6 The building step, with the refused standing and the run again
- [x] 6.7 The pointing step, with the command each picked harness needs
- [x] 6.8 The waiting step, and the note that meets the first served request
- [x] 6.9 Specify the celebration bursting on the note, from the one burst the checklist already
      uses

## 7. Placement and composition

- [x] 7.1 Move the harness catalog and its connect facts to `entities/harness`
- [x] 7.2 Move the provider catalog and its leads to `entities/provider`
- [x] 7.3 Hand the providers page's connect sheet to setup from the shell
- [x] 7.4 Land the shell on the gateway setup stored, and fold the checklist away
- [x] 7.5 Lift the chamfer and the confetti burst into `shared/ui`, with stories for each
- [x] 7.6 Prove the placement with `steiger`, `knip`, `jscpd`, and the boundaries gate

## 8. The suites

- [x] 8.1 Stories for every component under a `ui/` segment, in both schemes, with axe on each
- [x] 8.2 Seed a settled profile before every e2e scenario, with `@fresh-profile` as the opt-out
- [x] 8.3 `first-session.feature`: the surface's standing, leaving, and the way back in
- [x] 8.4 `setup-run.feature`: the counting, the catalog, and the step that refuses nothing marked
- [x] 8.5 Records 0188, 0189 and 0190, indexed
