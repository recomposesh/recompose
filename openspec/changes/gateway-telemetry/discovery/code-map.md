# Code map

- `openspec/changes/gateway-telemetry/specs/gateway-telemetry/spec.md` (openspec change) — symbols: none. The five requirements under change: status footer, footer-click drawer, streaming rows, filter chips, selection scoping.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-canvas-page/gateway-canvas-page.tsx` (pages/gateway-canvas (ui)) — symbols: GatewayCanvasPage. The composition root that must gain the status footer under GatewayStage and the logs drawer over the lower canvas.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-canvas-page/use-gateway-canvas.ts` (pages/gateway-canvas (ui)) — symbols: useGatewayCanvas, ComposedCanvas, PickerOnCanvas, RemovalAsked. Assembles flow, selection, and subject for the page; the footer tally and the drawer scope both read from what it returns.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-canvas-page/canvas-wiring.ts` (pages/gateway-canvas (ui)) — symbols: subjectOf, modelIdOf, editingText. Turns a raw canvas selection id into the subject the drawer scopes its rows by.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-canvas-page/canvas-standings.ts` (pages/gateway-canvas (ui)) — symbols: useCanvasStandings, CanvasStandings, CanvasWorld, PickerStanding. Holds the canvas selection state that the drawer's scope rule keys off.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-drawer/gateway-drawer.tsx` (pages/gateway-canvas (ui)) — symbols: GatewayDrawer, InspectorSubject. The existing right-side inspector; InspectorSubject ('gateway'|'virtual-model'|'cable'|'target'|'ghost-target'|'draft') is the scoping vocabulary, and the name collides with the new logs drawer.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/gateway-stage/gateway-stage.tsx` (pages/gateway-canvas (ui)) — symbols: GatewayStage, CanvasFlowWiring. The canvas section the footer sits under and the drawer overlays; it owns no state, so the footer cannot read counts from it.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/lib/node-graph.ts` (pages/gateway-canvas (lib)) — symbols: canvasGraph, CanvasGraph, CanvasNode, CanvasNodeKind, CanvasEdge, CableStanding, CableFailure. The derived graph whose nodes and edges are the only honest source for the footer's node and wire tally.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/cable-failure-chip/cable-failure-chip.tsx` (pages/gateway-canvas (ui)) — symbols: CableFailureChip. Existing failure presentation on a cable; a failed log row should read consistently with it.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/ui/binding-cable/binding-cable.tsx` (pages/gateway-canvas (ui)) — symbols: BindingCable. The only current consumer of live traffic in the renderer, colouring a cable from the latest outcome.
- `apps/desktop/src/renderer/src/pages/gateway-canvas/index.ts` (pages/gateway-canvas (public API)) — symbols: GatewayCanvasPage. The slice barrel; anything the route needs to reach must be exported here.
- `apps/desktop/src/renderer/src/shared/api/engine.ts` (shared/api) — symbols: engineTrafficQueryOptions, bindEngineTrafficToCache, engineStatesQueryOptions, gatewayStateIn, bindEngineStatesToCache, useStartGateway, useStopGateway. The push-fed query-cache pattern (skipToken plus initialData plus a bind function) that a telemetry and log-row channel must copy.
- `apps/desktop/src/renderer/src/shared/api/index.ts` (shared/api (public API)) — symbols: none. Segment barrel re-exporting ./engine; a new telemetry module registers here.
- `apps/desktop/src/renderer/src/shared/ui/chip/chip.tsx` (shared/ui) — symbols: Chip. The existing chip primitive with selected/onSelectedChange, the natural base for the all/errors/per-virtual-model filter chips.
- `apps/desktop/src/renderer/src/shared/ui/status-chip/status-chip.tsx` (shared/ui) — symbols: StatusChip. Existing status presentation, a candidate for the row's status-code marking.
- `apps/desktop/src/renderer/src/shared/ui/index.ts` (shared/ui (public API)) — symbols: Chip, StatusChip, Badge, Icon, SegmentedControl, PanelSeparator. Segment barrel; any new footer or drawer primitive lands here beside its ui/<name>/<name>.tsx folder and story.
- `apps/desktop/src/renderer/src/shared/lib/inspector-visibility.ts` (shared/lib) — symbols: inspectorOpen, subscribeToInspectorVisibility, toggleInspector. The external-store open/close pattern the logs drawer's open state should follow rather than invent.
- `apps/desktop/src/renderer/src/shared/lib/panel-width.ts` (shared/lib) — symbols: panelWidth, setPanelWidth, keepPanelWidth, subscribeToPanelWidths. Persisted panel sizing, the model for a remembered drawer height.
- `apps/desktop/src/renderer/src/shared/lib/panel-resize.ts` (shared/lib) — symbols: panelBounds, draggedPanel, restoredPanel, steppedPanel, PanelBounds. Resize bounds vocabulary; panelBounds currently names only the inspector, so a drawer bound is new.
- `apps/desktop/src/renderer/src/shared/lib/index.ts` (shared/lib (public API)) — symbols: none. Segment barrel the page imports through; a drawer-visibility store registers here.
- `apps/desktop/src/renderer/src/shared/testing/fake-gateways.ts` (shared/testing) — symbols: emitEngineTraffic, listenForEngineTraffic, forgetEngineTrafficListeners, gatewaySeed. How a browser test pushes a traffic snapshot; log rows and aggregates need the twin of these three.
- `apps/desktop/src/renderer/src/shared/testing/fake-bridge.ts` (shared/testing) — symbols: installFakeBridge, BridgeParameters. The fake bridge every browser test and story installs; its frozen recomposeEvents map gains the new channel.
- `apps/desktop/src/renderer/src/shared/testing/index.ts` (shared/testing (public API)) — symbols: none. Testing barrel that re-exports emitEngineStates, emitEngineTraffic, and gatewaySeed.
- `apps/desktop/src/renderer/src/app/routes/__root.tsx` (app/routes) — symbols: Route. Binds the traffic push into the query cache on mount via bindEngineTrafficToCache; a telemetry or logs push binds in the same effect.
- `apps/desktop/src/renderer/src/app/routes/gateways.$slug.tsx` (app/routes) — symbols: Route. The gateway detail route that renders GatewayCanvasPage; the surfaces land inside the page, not here.
- `apps/desktop/src/renderer/src/widgets/gateway/toolbar/ui/toolbar-footer/toolbar-footer.tsx` (widgets/gateway/toolbar (ui)) — symbols: ToolbarFooter. Already called a footer but carries the start-failure line under the toolbar; the new status footer must not reuse this name.
- `apps/desktop/src/renderer/src/widgets/status-bar/ui/status-bar/status-bar.tsx` (widgets/status-bar (ui)) — symbols: StatusBar. The app-wide status bar, distinct from the per-gateway canvas footer; naming and placement need deliberate separation.
- `apps/desktop/src/renderer/src/widgets/gateway/toolbar/index.ts` (widgets/gateway/toolbar (public API)) — symbols: GatewayToolbar. Toolbar slice barrel, the reference for how a gateway widget exposes one component.
- `packages/contracts/src/engine-traffic.ts` (contracts package (outside renderer FSD)) — symbols: requestOutcomeSchema, RequestOutcome, gatewayTrafficSchema, GatewayTraffic. Today's traffic contract is latest-outcome-per-virtual-model only; it carries no history, method, resolved provider model, account, duration, tokens, or cost, so the row and aggregate schemas are net new here.
- `packages/contracts/src/engine-protocol.ts` (contracts package (outside renderer FSD)) — symbols: engineTrafficReportSchema, EngineTrafficReport, engineSpendRequestSchema, EngineSpendRequest, engineReportSchema, EngineReport, spendGrantSchema. The child-to-parent wire; a structured log-row report joins engineTrafficReportSchema, and spendGrantSchema already names provider and accountId.
- `packages/contracts/src/ipc.ts` (contracts package (outside renderer FSD)) — symbols: ipcEvents, IpcEvent, IpcEventPayload, RecomposeIpcEvents, RecomposeIpc. The event-channel registry where 'engine:traffic' is declared; a logs or telemetry channel is added to ipcEvents.
- `packages/contracts/src/index.ts` (contracts package (outside renderer FSD)) — symbols: none. Package barrel both the main process and the renderer import from.
- `packages/engine/src/gateway-traffic.ts` (engine package (outside renderer FSD)) — symbols: watchingTraffic, NoteTraffic, ServeWatched. The single point where a serving turn is observed against its answer; duration, status, method, and the resolved provider model must be captured here.
- `packages/engine/src/gateway-app.ts` (engine package (outside renderer FSD)) — symbols: createGatewayApp, SpendGrantFor. Wires watchingTraffic and registerManagementLogs into the Hono app and holds the optional note callback.
- `packages/engine/src/engine-runtime.ts` (engine package (outside renderer FSD)) — symbols: createEngineRuntime, EngineRuntime, OpenListeners. Carries the NoteTraffic callback down into each gateway app.
- `packages/engine/src/engine-child.ts` (engine package (outside renderer FSD)) — symbols: attachEngineChild. Turns a note into a traffic report on the parent port; a log-row report leaves the child on this same path.
- `packages/engine/src/management-logs.ts` (engine package (outside renderer FSD)) — symbols: registerManagementLogs. Existing HTTP log endpoints over ProviderLogStore, serving raw text lines rather than structured rows.
- `packages/engine/src/provider/provider-log-store.ts` (engine package (outside renderer FSD)) — symbols: ProviderLogStore, LogRead. Line-based log persistence with cursor paging; adjacent prior art that carries no virtual model, account, or duration field.
- `packages/engine/src/management-usage.ts` (engine package (outside renderer FSD)) — symbols: registerManagementUsage. Existing usage endpoint, the likeliest source for the footer's tokens-per-minute and cost aggregates.
- `apps/desktop/src/main/engine-host/traffic-ledger.ts` (main process (outside renderer FSD)) — symbols: openTrafficDesk, TrafficDesk, TRAFFIC_PUSH_MS. The 16 ms coalescing desk that folds per-request words into one snapshot per frame; a row stream needs the same batching or it repaints per request.
- `apps/desktop/src/main/engine-host/engine-host.ts` (main process (outside renderer FSD)) — symbols: createEngineHost, DIRECTIVE_TIMEOUT_MS. Owns the traffic desk and routes every child report through it.
- `apps/desktop/src/main/engine-host/engine-host-types.ts` (main process (outside renderer FSD)) — symbols: EngineHostDeps, EngineHost, EngineChild. EngineHostDeps carries the optional onTraffic callback; a log-row push callback joins it here.
- `apps/desktop/src/main/ipc/push-events.ts` (main process (outside renderer FSD)) — symbols: pushEngineTraffic. Sends the traffic snapshot to every window; the pattern a logs push follows.
- `apps/desktop/src/main/index.ts` (main process (outside renderer FSD)) — symbols: none. Passes onTraffic: pushEngineTraffic into createEngineHost; the wiring point for any second push.
- `apps/desktop/src/preload/index.ts` (preload (outside renderer FSD)) — symbols: none. Exposes the frozen recomposeEvents map over contextBridge; a new channel must be added there or the renderer cannot subscribe.
- `openspec/specs/gateway-canvas/spec.md` (openspec living spec) — symbols: none. GAP: grep for footer, logs drawer, and telemetry returns nothing, so both surfaces are net new rather than extensions of a documented behaviour.
- `docs/assets/gateway-canvas.png` (docs asset) — symbols: none. GAP: the only canvas image in the repository; the reference screenshots the manifest cites for the footer layout are not committed anywhere I could find, so the footer's visual arrangement has no in-repo source.
