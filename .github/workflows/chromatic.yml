name: chromatic

on:
  push:
    branches: ['**']
  workflow_dispatch:
    inputs:
      accept-reviewed-baselines:
        description: accept this branch's reviewed snapshots as baselines
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: chromatic-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  chromatic:
    runs-on: ubuntu-latest
    steps:
      - uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
          persist-credentials: false
      - id: filter
        uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4
        with:
          predicate-quantifier: every
          filters: |
            desktop:
              - '{apps/desktop/**,packages/**,pnpm-lock.yaml,package.json,pnpm-workspace.yaml,turbo.json,vitest.shared.ts,tsconfig*.json,.github/workflows/chromatic.yml}'
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile --trust-lockfile
      - uses: chromaui/action@14cfaef73576e69f95f47f60058063f46ca38719 # v18.1.0
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          workingDir: apps/desktop
          skip: ${{ steps.filter.outputs.desktop != 'true' }}
          buildScriptName: storybook:build
          exitZeroOnChanges: true
          autoAcceptChanges: ${{ github.event_name == 'workflow_dispatch' && inputs.accept-reviewed-baselines && github.ref_name || 'main' }}
