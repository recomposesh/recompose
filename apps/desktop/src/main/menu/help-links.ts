/**
 * @summary Main-side constants rather than contract members, because one consumer reads them and
 * neither address crosses the wire.
 */
export const HELP_SITE_URL = 'https://recompose.sh';

export const NEW_ISSUE_URL = 'https://github.com/recomposesh/recompose/issues/new';
